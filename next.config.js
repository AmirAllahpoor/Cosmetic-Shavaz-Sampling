/** @type {import('next').NextConfig} */

const nextConfig = {
    images : {
        domains : ["files.edgestore.dev","avatars.githubusercontent.com","images.macrumors.com" ]
    }
}

module.exports = nextConfig